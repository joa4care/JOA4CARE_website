import { UploadImageActivitie } from './upload-image-activitie';

describe('UploadImageActivitie', () => {
  it('should create an instance', () => {
    expect(new UploadImageActivitie()).toBeTruthy();
  });
});
